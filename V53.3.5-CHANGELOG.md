# v53.3.5 — Homeroom conduct live sync

## Sửa lỗi chính

- Sửa lỗi sau khi thêm vi phạm học sinh nhưng bảng **Nề nếp 2026–2027 & gợi ý xếp loại** vẫn hiện `0`.
- Nguyên nhân: `homeroomActiveBook()` lấy một book object, sau đó `homeroomEnsureState()` chuẩn hóa workspace thành object mới; bản ghi bị đẩy vào tham chiếu book cũ nên không đi vào state đang render/lưu.
- Luồng thêm ghi nhận nay lấy `data` và `book` từ cùng một snapshot, sau đó commit lại vào workspace năm học trước khi render và persist.
- Ghi nhận tự do có `Nhóm ghi nhận = Vi phạm` nay vẫn được tính vào **Lỗi HK** và **Chưa xử lý**, nhưng không tự sinh điểm/hình thức quy chế nếu chưa gắn điều khoản chính thức.

## Tương thích

- Không đổi schema dữ liệu.
- Giữ nguyên toàn bộ dữ liệu và chức năng của v53.3.4.
