# V53.0 — STABLE

- Chốt kênh phát hành `stable`, giữ nguyên data schema 4 và backup version 7.
- Thêm Safe Boot Guard: nếu một tab khởi động lỗi lặp lại, lần sau tự giảm tải module phụ để bảo vệ phần lõi.
- Có thể ép chế độ an toàn bằng `?safe=1`; dữ liệu nghiệp vụ không bị thay đổi.
- Tạo `window.__teacherNotebookBootReport` để chẩn đoán thời gian init/lỗi init mà không gửi telemetry ra ngoài.
- Thêm `release-manifest.json` làm nguồn version/capability cho đóng gói và kiểm tra release.
- Safe mode không preload diagnostics khi idle; người dùng vẫn mở các module phụ theo nhu cầu.
- Tổng hợp các nâng cấp 52.5–52.9 thành một mốc ổn định dài hạn.
