# Sổ Tay Giáo Viên Pro v53.3 — Bảng thi đua lớp tự động

## Mục tiêu
Nâng tiếp trực tiếp từ v53.2 STABLE, giữ nguyên toàn bộ dữ liệu và chức năng cũ. Bản v53.3 bổ sung bảng thi đua lớp theo tuần, tháng, học kỳ (tham khảo) và năm học, tận dụng các ghi nhận nề nếp đã có trong Sổ chủ nhiệm.

## Điểm mới
- Bảng **Thi đua tuần**: Điểm cờ đỏ + điểm tổ giám thị + điểm Sổ đầu bài.
- Có thể tự cộng/trừ các lỗi nề nếp đã ghi trong Sổ chủ nhiệm vào điểm cờ đỏ; có nút tắt tự động để tránh cộng trùng nếu nhà trường đã chốt điểm cờ đỏ cuối cùng.
- Tách các khoản được dự thảo quy định tính vào **điểm tháng** khỏi điểm tuần để giảm nguy cơ cộng trùng.
- Tuần nằm giữa hai tháng được tự động tính vào tháng sau theo ngày cuối tuần.
- Bảng **Thi đua tháng**: TB các tuần đã lưu + thưởng/phạt tháng + điểm chăm sóc bồn hoa.
- Chăm sóc bồn hoa hỗ trợ hai lần chấm giữa/cuối tháng; nếu không nhập “điểm dùng tính tháng”, hệ thống dùng TB các lần đã nhập như một quy ước kỹ thuật và ghi rõ đây không phải công thức được nêu trong dự thảo.
- Theo dõi điểm trừ **cơ sở vật chất** và **lao động** theo tháng để tính điểm năm.
- Bảng **Học kỳ** chỉ hiển thị TB tuần tham khảo vì dự thảo không quy định công thức thi đua học kỳ riêng.
- **Điểm thi đua năm** = TB các tháng có dữ liệu + tổng điểm trừ CSVC + tổng điểm trừ lao động.
- Xuất Excel thêm 3 sheet: `Thi đua tuần`, `Thi đua tháng`, `Thi đua HK & năm`.
- Dữ liệu mới lưu tại `book.competition`, tương thích ngược với toàn bộ sổ v53.2.

## Kỹ thuật
- APP_VERSION / Service Worker / release manifest: `53.3.0`.
- Homeroom workspace schema nội bộ: `version: 6`.
- Không migration phá hủy dữ liệu; `normalizeHomeroomCompetition()` chỉ bổ sung cấu trúc mặc định khi thiếu.
- Module mới: `assets/js/36-homeroom-competition-v53-3.js`.
- Stylesheet mới: `assets/css/homeroom-v53-3.css`.
