# v50.6.0 — Trạng thái số tiết còn lại theo học kỳ

## Mục tiêu
Đổi cách hiển thị cột **Trạng thái** trong bảng Tiến độ: thay vì nhắc lại “Chậm/Nhanh bao nhiêu tiết” (đã có ở cột Chênh lệch), hiển thị số tiết còn phải học đến mốc phù hợp.

## Quy tắc mới
- Trong **HKI**: `Còn lại = Tiết PPCT kết thúc HKI đã xác nhận − PPCT thực tế đã học`.
- Khi đã sang **HKII**: `Còn lại = Tiết PPCT cuối cùng của cả năm − PPCT thực tế đã học`.
- Nếu chưa xác nhận mốc HKI thì trong HKI hiển thị **Chưa xác nhận mốc HKI**, không tự đoán.
- Khi đạt mốc HKI hiển thị **Đã hoàn thành mốc HKI**; khi đạt tiết cuối năm hiển thị **Đã hoàn thành PPCT cả năm**.
- Cột **Chênh lệch** vẫn giữ nguyên để giáo viên thấy đang nhanh/chậm so với PPCT dự kiến.

## Kỹ thuật
- Thêm helper dùng chung `buildSemesterRemainingStatus()` trong Shared Core.
- `buildCourseProgressRow()` giữ `status`/`paceLabel` nội bộ cho phân loại màu và cảnh báo, nhưng `statusLabel` dùng số tiết còn lại.
- Dashboard, Hệ thống gợi ý, Nhắc việc, Báo cáo và Hồ sơ tự động tự nhận `statusLabel` mới từ cùng một snapshot.
- Regression Test và fixture bổ sung trường hợp: 140 tiết cả năm, HKI kết thúc tiết 54, đã học 28 → còn 26 tiết HKI; HKII đã học 80 → còn 60 tiết đến hết năm.
- APP_VERSION / Service Worker: **50.6.0**; `DATA_SCHEMA_VERSION` giữ nguyên.
