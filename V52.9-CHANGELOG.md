# V52.9 — Đồng bộ bền vững

- Thêm “sync outbox” nhẹ trong LocalStorage để ghi nhớ trạng thái có thay đổi chưa đồng bộ.
- Khi mất mạng, dữ liệu vẫn lưu cục bộ và cloud badge hiển thị rõ “sẽ tự đồng bộ khi có mạng”.
- Khi mạng trở lại, hệ thống tự flush thay đổi đang chờ hoặc kích hoạt lại workspace cloud nếu cần.
- Khi lỗi mạng/xung đột, outbox không bị xóa; chỉ xóa sau khi đồng bộ thành công và không còn conflict.
- Outbox chỉ lưu metadata năm học/thời điểm, không sao chép dữ liệu học sinh hay API key.
- Giữ nguyên cơ chế revision transaction và xử lý conflict hiện có.
