# v50.3.0 — Khử trùng lặp Nhắc việc & Hệ thống gợi ý

## Mục tiêu
Loại bỏ tình trạng cùng một cảnh báo như thiếu Kế hoạch, thiếu TKB hoặc chưa có Lịch báo giảng xuất hiện hai lần liên tiếp trong Sổ Công Việc.

## Thay đổi
- Thêm quy tắc `isSystemSuggestionReminderWorthy()`:
  - `urgent` / `high` → Nhắc việc thông minh.
  - `normal` → Hệ thống gợi ý bổ sung.
- Thêm `getSmartReminderManagedSuggestionKeys()` để Sổ Công Việc biết các gợi ý đã do Reminder quản lý.
- `renderWorkSuggestions()` loại các key đã thuộc Reminder trước khi render.
- Nếu không còn gợi ý bổ sung, `workSuggestionPanel` tự ẩn.
- Khối gợi ý mặc định `hidden` để tránh chớp nội dung trùng trong lúc trang vừa khởi động.
- Loại bỏ markup mô tả Reminder bị lặp hai lần.
- Regression Test bổ sung kiểm tra de-dup Reminder/Gợi ý.
- Static Audit bổ sung guard chống tái phát logic trùng lặp.
- APP_VERSION / Service Worker → `50.3.0`.

## Không thay đổi
- DATA_SCHEMA_VERSION.
- Firebase / Firestore Rules.
- IndexedDB.
- Engine `buildWorkSystemSuggestions()`.
- Pipeline `addWorkSystemSuggestion()`.
