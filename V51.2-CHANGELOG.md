# v51.2.0 — So sánh & cập nhật lịch công tác điều chỉnh

## Mục tiêu
Khi tải một ảnh Lịch công tác của **tuần đã có dữ liệu**, hệ thống không ghi đè ngay. Ảnh mới được nhận dạng, so sánh theo từng ô và chỉ cập nhật sau khi giáo viên duyệt.

## Luồng mới
1. Tải ảnh lịch công tác như bình thường.
2. Nếu tuần chưa có: lưu như một tuần mới.
3. Nếu tuần đã có: mở cửa sổ **Lịch công tác điều chỉnh**.
4. So sánh `Tiêu đề`, `Trực tuần`, và 3 vùng của từng ngày: `Buổi sáng`, `Buổi chiều`, `Đi công tác`.
5. Phân loại thay đổi: **Thêm mới / Thay đổi / Bỏ**.
6. Giáo viên có thể chọn từng thay đổi, chọn tất cả, dùng toàn bộ ảnh mới, hoặc hủy để giữ nguyên bản cũ.
7. Chỉ sau khi xác nhận mới ghi dữ liệu và đánh dấu Lịch báo giảng liên quan cần đồng bộ lại.

## An toàn dữ liệu
- Không ghi đè bản tuần cũ trước khi duyệt.
- Có fingerprint chống ghi đè nếu kế hoạch dùng chung thay đổi trong lúc đang mở cửa sổ so sánh.
- Nếu ảnh mới có cảnh báo OCR/Gemini, các mục **Bỏ** không được chọn mặc định để tránh xóa nhầm do nhận dạng thiếu.
- Nếu nhận dạng ảnh điều chỉnh thất bại khi đã có dữ liệu, bản đang lưu được giữ nguyên; không tạo một tuần trống thay thế.
- Lưu tối đa 20 bản ghi lịch sử điều chỉnh dạng tóm tắt trong từng tuần.

## Giao diện
- Modal so sánh 5 cột: Chọn · Vị trí · Loại · Bản đang lưu · Ảnh mới.
- Badge tổng hợp số mục thêm/sửa/bỏ.
- Danh sách tuần có chip `🔄 Điều chỉnh`.
- Chi tiết tuần hiển thị lần điều chỉnh gần nhất, thời điểm và tên file ảnh.

## Kỹ thuật
- APP_VERSION: `51.2.0`
- DATA_SCHEMA_VERSION: giữ nguyên `1`.
- Module mới: `assets/js/11-plan-revision.js`.
- CSS mới: `assets/css/plan-revision-v51.css`.
- Service Worker app-shell đã bổ sung hai tài nguyên trên.
