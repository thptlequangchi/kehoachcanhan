# v53.3.4 — OCR Offline & Durable Delete

## Sửa lỗi nhận dạng khi mất mạng
- Tesseract OCR lấy thêm TSV/tọa độ từ ảnh.
- Thêm bộ dựng thời khóa biểu theo vị trí: phát hiện header Thứ 2–7, hàng tiết 1–5 và hai vùng sáng/chiều.
- Kết quả có độ tin cậy bố cục và cảnh báo khi một số mốc bị mờ.
- Service Worker làm ấm OCR runtime + `vie`/`eng` traineddata khi có mạng để dùng cho lần offline sau.

## Sửa lỗi xóa rồi xuất hiện lại
- Không lưu cache TKB trắng hoặc nhận dạng thủ công.
- Recognition Engine tăng lên 5 để bỏ cache lỗi cũ.
- Xóa TKB tuần đồng thời xóa cache nhận dạng của ảnh nguồn.
- Cloud sync có dirty/pending hash, bỏ qua snapshot cũ trong lúc thay đổi local đang chờ ghi.
- Ghi personal year workspace bằng snapshot đầy đủ thay vì merge sâu, bảo đảm khóa tuần đã xóa cũng mất trên Firestore.

## Tương thích
- Kế thừa toàn bộ v53.3.3, đặc biệt chuỗi PPCT chung cho HĐTN_SHDC/HĐTN_SHL.
- Không xóa hoặc reset dữ liệu người dùng khi nâng cấp.
