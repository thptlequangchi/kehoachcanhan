# v50.1.0 — Làm sạch & hợp nhất logic

## Mục tiêu
Giảm trùng lặp logic sau nhiều bước nâng cấp mà không thay đổi dữ liệu, quyền truy cập hay nghiệp vụ giáo viên.

## Thay đổi chính
- Sửa lỗi Overview có thể hiểu sai lịch đã chốt do dùng `finalized` trong khi trạng thái chuẩn là `final`.
- Thêm `assets/js/04-shared-core.js` làm nguồn dùng chung cho:
  - trạng thái tuần Kế hoạch → TKB → Lịch báo giảng;
  - trạng thái lịch đã chốt;
  - danh sách tiết dạy hôm nay;
  - nhiệm vụ chưa hoàn thành;
  - tải Blob/file;
  - chế độ in báo cáo;
  - refresh dữ liệu và heartbeat mỗi phút.
- Overview, Trợ lý tuần, Dashboard năm học và gợi ý Sổ Công Việc cùng đọc `getWeekOperationalStatus()` thay vì tự viết lại quy tắc.
- 8 listener `teacher-data-changed` độc lập được gom về 1 bộ điều phối dùng chung.
- 4 timer 60 giây độc lập được gom về 1 heartbeat dùng chung.
- Gom thao tác áp workspace vào runtime bằng `applyYearWorkspaceToRuntime()` cho chuyển năm, Firebase và Restore.
- Gom luồng tải Blob của Backup, Lịch báo giảng, Health Check, Hồ sơ, Reminder và Regression.
- Gom thao tác bật/tắt `print-report-mode` cho Report Center và Hồ sơ tự động.
- Dọn CSS responsive bị chồng `order`/`!important` ở nhóm liên kết nhanh trên header.

## An toàn dữ liệu
- `DATA_SCHEMA_VERSION` giữ nguyên.
- Không migration dữ liệu.
- Không đổi Firestore Rules.
- Không đổi cấu trúc IndexedDB.
- Không xóa chức năng hay thay đổi quyền người dùng.
